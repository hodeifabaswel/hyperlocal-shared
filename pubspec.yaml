name: hyperlocal_shared
description: Shared UI components, services, and models for Hyperlocal Customer & Mitra apps.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.7.0
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  package_info_plus: ^8.0.0
  device_info_plus: ^10.0.0
  dio: ^5.7.0
  jwt_decoder: ^2.0.1
  permission_handler: ^11.3.0
  flutter_map: ^7.0.0
  latlong2: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
